import React from 'react'
import { View, Text } from 'react-native'

interface Props {
  
}

const Home = (props: Props) => {
  return (
    <View testID='home'>
      <Text>HOME</Text>
    </View>
  )
}

export default Home
